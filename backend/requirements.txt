fastapi==0.110.1
uvicorn[standard]==0.29.0
aiosqlite==0.20.0
pydantic==2.6.4
pydantic-settings==2.2.1
python-dotenv==1.0.1

# Database — SQLAlchemy core + Alembic migrations + Postgres driver.
# psycopg[binary] is the v3 driver; SQLAlchemy URL is `postgresql+psycopg://...`.
sqlalchemy>=2.0,<2.1
alembic>=1.13,<1.14
psycopg[binary]>=3.1,<4

# Market data — Upstox (authenticated primary) → NSE → jugaad-data → yfinance
# → Twelve Data. Upstox + Twelve Data are plain HTTP (requests), no extra SDK.
# jugaad-data is a free, account-less NSE EOD source sturdier than NSE's
# anti-bot live API.
nse[local]>=2.1.3
yfinance==0.2.38
jugaad-data>=0.25

# Broker SDKs are *optional* — see requirements-broker.txt. They're loaded
# lazily inside app/services/broker.py and the adapter fails gracefully
# when missing, so the core backend boots fine without them. Install only
# when you've wired credentials in Settings → Broker.
pandas==2.2.1
numpy==1.26.4
ta>=0.11.0
TA-Lib>=0.6.5

# News & sentiment
feedparser==6.0.11
requests==2.31.0
beautifulsoup4==4.12.3

# LLM providers
openai>=1.35.0
anthropic>=0.40.0
google-genai>=1.0.0

# Caching (optional)
redis==5.0.3

# Scheduler
apscheduler==3.10.4

# HTTP client for testing
httpx==0.27.0

# Testing extras
pytest-mock>=3.14.0

# Secrets at rest — Fernet symmetric encryption for SECRET_KEYS settings
cryptography>=42.0,<46.0

# Meta-labeling + gradient-boosted factor models (López de Prado, AFML Ch.3).
# Optional at runtime — when missing, services fall back to logistic regression.
scikit-learn>=1.3,<2.0
